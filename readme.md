
```sh
pip install -r requirements.txt
python git-collate.py <path to project directory> <file extension>
```

```
python setup.py install
git collate <path to project directory> <file extension>
```

